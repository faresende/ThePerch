# ─── Local workflow state ─────────────────────────────────────────────────
.bancada/

# ─── Secrets: Supabase, iOS, Python, private keys ─────────────────────────
# iOS Secrets.plist (legacy local secret store; do not commit).
ios/ThePerch/Sources/ThePerch/Config/Secrets.plist
Secrets.plist

# iOS xcconfig: never commit the real one. Commit only the .example file.
*.xcconfig
!*.example.xcconfig

# Python and tooling .env: commit only .env.example.
.env
.env.*
!.env.example
!.env.*.example

# Private keys and certificates of any kind.
*.p8
AuthKey_*.p8
*.p12
*.pem
*.key
id_rsa
id_rsa.*
id_ecdsa
id_ecdsa.*
id_ed25519
id_ed25519.*

# Cloud service accounts.
service-account*.json
service_account*.json
gcp-*.json
firebase-adminsdk*.json

# ─── macOS ────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ─── Xcode ────────────────────────────────────────────────────────────────
*.xcuserdata
*.xcuserdatad/
DerivedData/
*.moved-aside
xcuserdata/
UserInterfaceState.xcuserstate
xcschememanagement.plist
*.xccheckout
*.xcscmblueprint

# Xcode build output
build/
.build/
ios/.build/

# ─── CocoaPods / Swift packages ───────────────────────────────────────────
Pods/

# ─── Supabase CLI ─────────────────────────────────────────────────────────
supabase/.temp/

# ─── Node / npm (skill/dashboard-sync etc.) ───────────────────────────────
node_modules/

# ─── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# ─── Editor junk ──────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ─── Historical one-off entries (kept for reference) ──────────────────────
docs/superpowers/plans/2026-03-29-nutrition-food-memory.md
